import Head from "next/head";

function Header() {
  return (
    <div>
      <Head>
        <title>Joseph Jo</title>
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />
      </Head>
    </div>
  );
}

export default Header;
